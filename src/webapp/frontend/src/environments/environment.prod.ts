/**
 * Production environment configuration.
 * This file is used when the application is built for production.
 */
export const environment = {
  /** Indicates if the application is running in production mode. */
  production: true,
  /** Base URL for the backend API. In production, this is usually relative. */
  apiUrl: '/api',
};
